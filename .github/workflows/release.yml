name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and publish"
        required: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  create-release:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
    steps:
      - uses: actions/checkout@v5
      - id: tag
        run: echo "tag=${{ github.event.inputs.tag || github.ref_name }}" >> "$GITHUB_OUTPUT"
      - uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          draft: false
          generate_release_notes: true

  upload:
    needs: create-release
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # musl on Linux, so the binary really is standalone. Every
          # dependency is pure Rust, so each target builds natively on a runner
          # of its own architecture and no cross-compiler is involved.
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
          - target: aarch64-unknown-linux-musl
            os: ubuntu-24.04-arm
          # Apple silicon only. Intel Macs are not a target worth carrying:
          # the runners for them are being retired, and anyone still on one can
          # build from source.
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      # Names the archive mdroll-<tag>-<target>.tar.gz, which is what mise's
      # `github:` backend matches on when picking an asset for the host.
      - uses: taiki-e/upload-rust-binary-action@v1
        with:
          bin: mdroll
          target: ${{ matrix.target }}
          # Without this the action reaches for `cross` on the musl targets,
          # which then tries to install an x86_64 toolchain on the arm64
          # runner and fails. The host already matches the target here.
          build_tool: cargo
          archive: $bin-$tag-$target
          include: README.md,LICENSE
          checksum: sha256
          ref: refs/tags/${{ needs.create-release.outputs.tag }}
          token: ${{ secrets.GITHUB_TOKEN }}
